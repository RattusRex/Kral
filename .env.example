# Copy this file to `.env` and adjust the values for your local setup.
# `npm run dev` and the FastAPI backend read DATABASE_URL from here automatically.

# PostgreSQL connection string used by the backend.
DATABASE_URL=postgresql://postgres:GalU5TA1@localhost:5432/EpohaTruda
